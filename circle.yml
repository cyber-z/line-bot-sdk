machine:
  node:
    version: 6.9.0
dependencies:
  override:
    - npm install --dev
test:
  override:
    - ./node_modules/.bin/eslint ./
    - npm test
  post:
    - bash <(curl -s https://codecov.io/bash)
